import { Kind } from './types';
import getCorners from './getCornets';
import { DEFAULT_PADDING } from './constants';
import isFocusSquareElem from './isFocusSquareElem';
export default function renderFigure(x, y, neighbors, cellSize, renderer, matrixFocusSquareDeep, i, j, matrixSize) {
  const padding = renderer.options?.padding ?? DEFAULT_PADDING;
  const corners = getCorners(x, y, cellSize, padding);
  const isSquareElem = isFocusSquareElem(i, j, matrixSize, matrixFocusSquareDeep);
  const params = {
    neighbors,
    corners,
    cellSize,
    padding,
    isSquareElem,
    i,
    j
  };
  if (!(neighbors.top || neighbors.right || neighbors.bottom || neighbors.left)) {
    return {
      type: 'circle',
      d: renderer.render[Kind.Circle](params)
    };
  }
  return {
    type: 'path',
    d: renderer.render[Kind.Element](params)
  };
}
//# sourceMappingURL=renderFigure.js.map