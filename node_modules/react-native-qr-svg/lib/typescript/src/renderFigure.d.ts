import { type CustomRenderer, type Neighbors } from './types';
export type Figure = {
    type: string;
    d: string;
};
export default function renderFigure(x: number, y: number, neighbors: Neighbors, cellSize: number, renderer: CustomRenderer, matrixFocusSquareDeep: number, i: number, j: number, matrixSize: number): Figure;
//# sourceMappingURL=renderFigure.d.ts.map